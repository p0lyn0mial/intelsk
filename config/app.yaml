app:
  host: 0.0.0.0
  port: 8000
  data_dir: data
  log_level: info

mlservice:
  url: http://localhost:8001

storage:
  db_path: data/intelsk.db

clip:
  batch_size: 32

process:
  history_path: data/process_history.json
